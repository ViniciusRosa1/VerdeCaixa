"use client";
import { useRouter } from "next/navigation";
import { useState, type FormEvent } from "react";
import { Card, CardContent, CardFooter } from "@/components/ui/card";
import { Button, Field, Input, Select, SelectItem, Textarea } from "@/components/ui/controls";
import { PageHeading } from "@/components/shared-pages";
import { decimal, valueOf } from "../common";
import { useCategories, useEntry, useProjects, useSaveEntry, useSuppliers } from "@/lib/api/hooks";
export function PagarForm({ id }: { id?: string }) { const router = useRouter(); const detail = useEntry(id); const save = useSaveEntry(id); const suppliers = useSuppliers(); const categories = useCategories(); const projects = useProjects(); const [plan, setPlan] = useState("CASH"); const [message, setMessage] = useState(""); if (id && detail.isLoading) return <p>Carregando despesa…</p>; const current = detail.data; async function submit(event: FormEvent<HTMLFormElement>) { event.preventDefault(); const form = new FormData(event.currentTarget); const body = id ? { description: valueOf(form, "description"), counterpartyId: valueOf(form, "counterpartyId"), categoryId: valueOf(form, "categoryId"), projectId: optional(form, "projectId"), notes: optional(form, "notes") } : { kind: "EXPENSE", plan, description: valueOf(form, "description"), counterpartyId: valueOf(form, "counterpartyId"), categoryId: valueOf(form, "categoryId"), projectId: optional(form, "projectId"), totalAmount: decimal(valueOf(form, "totalAmount")), dueDate: valueOf(form, "dueDate"), installmentCount: plan === "INSTALLMENT" ? Number(valueOf(form, "installmentCount")) : undefined, recurrenceEndsOn: plan === "RECURRING" ? optional(form, "recurrenceEndsOn") : undefined, notes: optional(form, "notes") }; try { await save.mutateAsync(body); router.push("/pagar"); } catch (error) { setMessage(error instanceof Error ? error.message : "Não foi possível salvar."); } } return <div className="page-enter mx-auto max-w-4xl"><PageHeading eyebrow="Financeiro" title={id ? "Editar despesa" : "Nova despesa"} description="Registre os compromissos e despesas da empresa." />{message && <p role="alert" className="mb-4 rounded-xl bg-surface p-3 text-sm text-danger">{message}</p>}<form onSubmit={submit} className="space-y-4"><Card><CardContent className="grid gap-5 sm:grid-cols-2"><Field label="Descrição" htmlFor="description"><Input id="description" name="description" required defaultValue={current?.description} /></Field>{!id && <Field label="Valor total" htmlFor="totalAmount"><Input id="totalAmount" name="totalAmount" inputMode="decimal" required /></Field>}<Field label="Fornecedor" htmlFor="counterpartyId"><Select name="counterpartyId" required defaultValue={current?.counterparty.id}>{(suppliers.data?.data ?? []).map((item) => <SelectItem key={item.id} value={item.id}>{item.name}</SelectItem>)}</Select></Field><Field label="Categoria" htmlFor="categoryId"><Select name="categoryId" required defaultValue={current?.category.id}>{(categories.data?.data ?? []).filter((item) => item.kind === "EXPENSE").map((item) => <SelectItem key={item.id} value={item.id}>{item.name}</SelectItem>)}</Select></Field><Field label="Projeto" htmlFor="projectId"><Select name="projectId" defaultValue={current?.project?.id ?? "none"}><SelectItem value="none">Sem projeto</SelectItem>{(projects.data?.data ?? []).map((item) => <SelectItem key={item.id} value={item.id}>{item.name}</SelectItem>)}</Select></Field>{!id && <Field label="Vencimento" htmlFor="dueDate"><Input id="dueDate" name="dueDate" type="date" required /></Field>}</CardContent></Card>{!id && <Card><CardContent className="grid gap-5 sm:grid-cols-2"><Field label="Forma do lançamento" htmlFor="plan"><Select name="plan" value={plan} onValueChange={setPlan}><SelectItem value="CASH">À vista</SelectItem><SelectItem value="INSTALLMENT">Parcelado</SelectItem><SelectItem value="RECURRING">Recorrente</SelectItem></Select></Field>{plan === "INSTALLMENT" && <Field label="Quantidade de parcelas" htmlFor="installmentCount"><Input id="installmentCount" name="installmentCount" type="number" min="2" max="120" defaultValue="3" /></Field>}{plan === "RECURRING" && <Field label="Término opcional" htmlFor="recurrenceEndsOn"><Input id="recurrenceEndsOn" name="recurrenceEndsOn" type="date" /></Field>}</CardContent></Card>}<Card><CardContent><Field label="Observações" htmlFor="notes"><Textarea id="notes" name="notes" defaultValue={current?.notes} /></Field></CardContent><CardFooter className="flex justify-end gap-2"><Button type="button" variant="secondary" onClick={() => router.push("/pagar")}>Cancelar</Button><Button disabled={save.isPending}>{save.isPending ? "Salvando…" : "Salvar despesa"}</Button></CardFooter></Card></form></div>; }
function optional(form: FormData, name: string) { const value = valueOf(form, name); return value && value !== "none" ? value : undefined; }
