"use client";
import { useRouter } from "next/navigation";
import { useState, type FormEvent } from "react";
import { FormPage, valueOf } from "../common";
import { CardContent } from "@/components/ui/card";
import { Checkbox, Field, Input } from "@/components/ui/controls";
import { usePermissions, useRole, useSaveRole } from "@/lib/api/entity-hooks";
export function PerfisForm({ id }: { id?: string }) { const router = useRouter(); const detail = useRole(id); const permissions = usePermissions(); const save = useSaveRole(id); const [message, setMessage] = useState(""); if (id && detail.isLoading) return <p>Carregando papel…</p>; const current = detail.data; async function submit(event: FormEvent<HTMLFormElement>) { event.preventDefault(); const form = new FormData(event.currentTarget); try { await save.mutateAsync({ name: valueOf(form, "name"), description: valueOf(form, "description") || undefined, permissions: form.getAll("permissions").map(String) }); router.push("/perfis"); } catch (error) { setMessage(error instanceof Error ? error.message : "Não foi possível salvar."); } } return <FormPage title={`${id ? "Editar" : "Novo"} papel`} backHref="/perfis" message={message} pending={save.isPending} onSubmit={submit}><CardContent className="grid gap-5 sm:grid-cols-2"><Field label="Nome" htmlFor="name"><Input id="name" name="name" required defaultValue={current?.name} /></Field><Field label="Descrição" htmlFor="description"><Input id="description" name="description" defaultValue={current?.description} /></Field><div className="sm:col-span-2"><p className="mb-2 text-xs font-semibold text-muted">Permissões</p><div className="grid gap-2 sm:grid-cols-2">{(permissions.data ?? []).map((permission) => <Checkbox key={permission.code} name="permissions" value={permission.code} label={permission.description} defaultChecked={current?.permissions.includes(permission.code)} />)}</div></div></CardContent></FormPage>; }
